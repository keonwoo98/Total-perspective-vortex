# Optional dependency for the bonus second dataset (tpv/external.py). NOT needed for the
# mandatory project. Install into a SEPARATE venv to avoid perturbing the verified pipeline:
#   python3.12 -m venv .venv-bonus
#   .venv-bonus/bin/pip install -r requirements.txt -r requirements-bonus.txt
moabb>=1.5.0
# moabb's core deps pinned to the mandatory windows so a bonus install can't shift them:
mne>=1.10,<1.13
scikit-learn>=1.5,<1.7
numpy>=1.26,<2.3
scipy>=1.13,<1.18
